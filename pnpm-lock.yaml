'use client'

import {
  ShieldCheck,
  Users,
  Award,
  Clock,
  HardHat,
  Smile,
  Briefcase,
  Heart,
  type LucideIcon,
} from 'lucide-react'
import { whyChooseUs } from '@/lib/company-data'
import { Reveal, RevealStagger, StaggerItem } from './reveal'

const icons: LucideIcon[] = [
  ShieldCheck,
  Users,
  Award,
  Clock,
  HardHat,
  Smile,
  Briefcase,
  Heart,
]

export function WhyChoose() {
  return (
    <section className="relative overflow-hidden bg-primary py-24 text-primary-foreground md:py-32">
      <div className="absolute inset-0 opacity-[0.04] [background-image:linear-gradient(to_right,white_1px,transparent_1px),linear-gradient(to_bottom,white_1px,transparent_1px)] [background-size:48px_48px]" />
      <div className="relative mx-auto max-w-7xl px-4 md:px-8">
        <Reveal>
          <div className="mx-auto max-w-2xl text-center">
            <p className="text-sm font-semibold uppercase tracking-[0.18em] text-accent">
              Why Choose Us
            </p>
            <h2 className="mt-3 text-balance font-heading text-3xl font-extrabold tracking-tight md:text-4xl">
              The competitive advantage behind every project
            </h2>
          </div>
        </Reveal>

        <RevealStagger className="mt-14 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">
          {whyChooseUs.map((item, i) => {
            const Icon = icons[i % icons.length]
            return (
              <StaggerItem key={item.title}>
                <div className="h-full rounded-2xl border border-white/10 bg-white/5 p-6 backdrop-blur-sm transition-colors hover:border-accent/40 hover:bg-white/10">
                  <span className="flex size-11 items-center justify-center rounded-xl bg-accent/15 text-accent">
                    <Icon className="size-5" />
                  </span>
                  <h3 className="mt-4 font-heading text-base font-bold">
                    {item.title}
                  </h3>
                  <p className="mt-2 text-sm leading-relaxed text-primary-foreground/70">
                    {item.description}
                  </p>
                </div>
              </StaggerItem>
            )
          })}
        </RevealStagger>
      </div>
    </section>
  )
}
